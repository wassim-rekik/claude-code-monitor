// Recharts consumes tick/style config as plain objects (not CSS classes), so
// this lives alongside the chart components rather than in a CSS module.
export const AXIS_TICK_STYLE = { fill: "var(--color-text-dim)", fontSize: 10, fontFamily: "var(--font-mono)" };
export const GRID_STROKE = "var(--color-border)";
