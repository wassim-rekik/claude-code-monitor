{
  "name": "claude-monitor",
  "version": "0.1.0",
  "description": "Background agent that streams Claude Code token usage to your server",
  "type": "module",
  "bin": {
    "claude-monitor": "./src/cli.js"
  },
  "files": ["src", "install.sh"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "chalk": "^5.3.0",
    "chokidar": "^3.6.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  }
}
